{
  "name": "iterum",
  "version": "0.0.1",
  "description": "Another attempt to create VM with its own language and intermediate representation",
  "main": "index.js",
  "keywords": [
    "iterum",
    "parser",
    "interpretation",
    "vm"
  ],
  "scripts": {
    "build": "npm run clean && npm run lint && npm run test && tsc",
    "clean": "rimraf dist",
    "lint": "tslint --project tsconfig.json --config tslint.json",
    "test": "jest --detectOpenHandles"
  },
  "author": "ghaiklor",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "24.0.6",
    "jest": "24.1.0",
    "rimraf": "2.6.3",
    "ts-jest": "24.0.0",
    "tslint": "5.12.1",
    "typescript": "3.3.3"
  }
}
